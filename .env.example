# Database
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=flight_watcher
POSTGRES_USER=flight_watcher
POSTGRES_PASSWORD=changeme

# Scanner
# Hour of day (UTC) to run the daily scan (0-23)
SCAN_HOUR_UTC=0
LOG_LEVEL=INFO

# Anti-detection delays (seconds)
MIN_DELAY_SEC=5
MAX_DELAY_SEC=15

# Circuit breaker
CB_FAILURE_THRESHOLD=3

HEALTH_PORT=8080
